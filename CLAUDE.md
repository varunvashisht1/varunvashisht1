# CLAUDE.md - varunvashisht1

My public GitHub profile README (github.com/varunvashisht1). One file matters: `README.md`, and
it renders on my profile the moment it lands on `main`.

## Rules

- **A push here is a publish.** The profile updates instantly, so the full-diff review before
  merging matters more than usual. Push only on my explicit ask.
- **Do not edit between the `BLOG-POST-LIST:START` and `END` markers.** A GitHub Action owns
  that block.
- **The live SVG card is served by a Worker** on varunvashisht.com, the README only embeds it.
  If the card is wrong, the fix is in the VarunVashisht-Web repo, not here.
- Voice: this is public professional copy. Writing rules apply in full, British English, no
  em-dashes, no semicolons, no AI-tell vocabulary, claims stay concrete (numbers, not
  adjectives).

## Git

Global rules apply: feature branch, full-diff review, merge --no-ff.
